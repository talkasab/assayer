- if show_title?
  %h1=h yield(:title)
%p 
  -if current_user.present?
    Logged in as #{current_user.name}.
    = link_to "Logout", destroy_user_session_path
  -else
    = link_to_unless_current "Login", new_user_session_path do
      &nbsp;
= yield(:header)
